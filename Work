Ce face proiectul AegisFS

AegisFS este un mini-scanner de fișiere bazat pe hash și reputație, cu interfață web și CLI.

Primește fișiere (din browser sau terminal)

Calculează hash-ul SHA-256 pentru fiecare fișier

Verifică dacă fișierul e suspect (hash blacklist sau API extern)

Returnează un verdict: clean / suspicious / malicious

Dashboard web afișează istoricul scanărilor și statistici

Nu modifică fișierele, doar le analizează și generează alerte.

🔹 Etapele proiectului
Etapa 1 – Detectarea fișierelor (Motorul)

Input: fișier/folder selectat

Calculează SHA-256

Decide dacă fișierul este curat sau suspect

Output: verdict în consolă
Skill nou: Node.js, manipulare fișiere, hashing

Etapa 2 – Reputatia fișierului (Creierul)

Opțional: verificare hash cu API extern (ex: VirusTotal)

Poate folosi un blacklist local pentru teste (ex: fișier EICAR)

Output: verdict mai precis
Skill nou: integrare API, gestionare erori rețea

Etapa 3 – Dashboard (Turnul de control)

Pagina web cu upload multiple fișiere

Serverul calculează hash și trimite verdict

Afișează: nume fișier, hash, verdict, dată scanare

Permite vizualizare rapidă a scanărilor
Skill nou: Node.js + Express + frontend HTML/JS, vizualizare date

Etapa 4 – CLI / Terminal

Permite utilizatorului să trimită fișiere sau folder local la server

Primește verdict în terminal

Poți testa rapid mai multe fișiere / batch
Skill nou: scripting CLI, interfață terminal

Etapa 5 – Extensii viitoare

Integrare completă VirusTotal API

Grafice statistici în dashboard

Client CLI care trimite automat foldere întregi

Deploy pe server public (Render, Railway) → link public

🔹 Flow-ul complet al aplicației

User alege fișiere (browser) sau trimite folder (CLI)

Server primește fișierele

Se calculează hash SHA-256

Se verifică reputația (local/extern)

Se returnează verdict

Dashboard afișează rezultatele și statistici